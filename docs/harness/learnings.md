# Harness Learnings

Este arquivo registra aprendizados técnicos do projeto que devem orientar futuros agentes.

## Formato

### YYYY-MM-DD - Título

- Contexto:
- Decisão:
- Evidência:
- Impacto para próximos agentes:

### 2026-06-30 - Slice 2-C descobre o mesmo anti-pattern jsonb de Slice 3-IT em nova coluna, opta por text + re-aproveita inspector pattern para HasWebhookSecret

- Contexto: Slice 2-C (2026-06-30) adiciona 4 colunas non-sensitive a `provider_accounts` para gerenciar a inscricao de webhook AbacatePay via API. O plano original marcava `webhook_events` como `jsonb` ("para queries SQL/GIN-index"), exatamente como o Slice 3-IT ja tinha feito (e revertido) para `webhook_events.raw_payload`. Apos a primeira execucao do teste `ProviderAccount_ShouldPersistAllWebhookConfigurationColumns`, a string retornada tinha um espaco extra apos cada `,` e `:` ("differs near t" no erro de FluentAssertions), confirmando que **Postgres `jsonb` reformata JSON no insert**, mesmo quando o caller envia um payload ja serializado em C#. Alem disso, o handler precisava responder `hasWebhookSecret: bool` em GET sem nunca expor o valor, replicando o padrao que `ProcessWebhookEventHandler.ExtractWebhookSecret` ja usava para resolver o segredo no caminho de webhook inbound.
- Decisao: (1) **Trocar `provider_accounts.webhook_events` para `text`** (mesma regra que `webhook_events.raw_payload`): o Postgres `jsonb` normaliza o JSON no insert (insere espaco apos cada `:`, `,`), o que quebraria qualquer round-trip byte-exact ou HMAC no body armazenado. Como `WebhookEvents` e usado apenas pelo parser de resposta (que desserializa em memoria via `JsonDocument.Parse`), `text` e o tipo correto. Migration `20260630001726_AddProviderAccountWebhookColumns` corrigida. Anti-regression note espelhada no comentario inline em `EntityConfigurations.cs` e na migration. (2) **Extrair `ProviderAccountCredentialsInspector`** (helper `internal static` em `Application/Tenants/Validation/`, mesmo pattern de `WebhookUrlValidator`) para o caminho de "unprotect + parse JSON + decidir se existe webhookSecret". O inspector e puro (sem DI, sem logging, sem exceptions). O handler e o GET response builder usam-no em vez de duplicar a logica de `ProcessWebhookEventHandler.ExtractWebhookSecret`. (3) **NUNCA usar `jsonb` para armazenar JSON** que o sistema insere via `JsonSerializer.Serialize(...)` e reconsome via `JsonDocument.Parse(...)`. O padrao "INSERT byte-exact + RECONSTROY in-memory" e o caso classico de `text`. Reservar `jsonb` apenas quando o SQL precisa consultar campos do JSON (GIN index, predicates). (4) **DTO de response nao expoe secret-shaped fields** (validado por reflexao em 3 testes distintos): nenhum getter chamado `ApiKey`, `WebhookSecret`, `ProtectedWebhookSecret`, `EncryptedCredentials`. O unico acesso ao segredo pelo consumidor da API e o boolean `hasWebhookSecret`. (5) **Feature flag opt-in + interface provider-agnostic em Application**: `Providers:AbacatePay:AllowWebhookRegistration` (default `false`) + `IProviderWebhookRegistrationFeaturePolicy` em `Application/Abstractions/Providers/`. Implementacao concreta em `Infrastructure.Providers.AbacatePay/` (`AbacatePayWebhookRegistrationFeaturePolicy`) + no-op `NoOpProviderWebhookManagementClient`. Tudo isso para que Application NAO dependa de Infrastructure (Clean Architecture) e para que o gate de "remote registration" seja sempre opt-in ate o Slice 2-C.1 substituir o no-op pelo client HTTP real.
- Evidencia: migration nova `20260630001726_AddProviderAccountWebhookColumns` (4 AddColumn, todos `text`/`varchar`/`timestamp`, zero `jsonb`). Migration Designer + ModelSnapshot regenerados pelo `dotnet ef migrations remove/add` (com fallback manual quando o tool nao conecta ao DB). Suite previa: 422 testes. Suite nova: **484 testes (+59 unit + 3 integration)**, todos passando. `dotnet build PaymentHub.slnx` 0/0 (9 projetos). `dotnet test PaymentHub.slnx` em ~6s. `dotnet test PaymentHub.IntegrationTests.csproj` 17 passando (14 Slice 1-IT + 3 Slice 2-C). Filtros `~AbacatePay`, `~ProviderAccount`, `~Webhook`, `~EndToEnd` verdes. `scripts/agent-architecture-check.sh` passou (Application continua sem depender de Infrastructure). Relatorio em `docs/audits/slice-2c-abacatepay-webhook-management-report-2026-06-30.md`. Specs `006`, `008`, `009`, `011` atualizadas com a nova secao. Validation matrix extendida com bloco Phase 2 Slice 2-C.
- Impacto para proximos agentes: (a) **Postgres `jsonb` reformata JSON no insert** — sempre que armazenar corpo bruto que precisa ser validado por HMAC, assinatura digital, hash ou checksum externo, OU que precisa ser reconsumido em memoria via `JsonDocument.Parse`, usar `text` (ou `json`, que preserva o original). `jsonb` so quando o payload precisa de queries SQL/GIN-index. **ANTI-PATTERN (DO NOT)**: reverter `provider_accounts.webhook_events` para `jsonb` em nova migration; o teste `ProviderAccountWebhookPersistenceTests.ProviderAccount_ShouldPersistAllWebhookConfigurationColumns` quebra com diff de whitespace. **ANTI-PATTERN (DO NOT)**: adicionar novas colunas `*_json` marcadas como `jsonb` para "conveniencia" — auditar sempre a politica de tipo de coluna quando uma migration nova tocar JSON-shaped data. (b) **Sempre usar `internal static` + `InternalsVisibleTo("PaymentHub.UnitTests")` para helpers puros de validacao/inspecao** (`ProviderAccountCredentialsInspector`, `WebhookUrlValidator`, etc.). Manter essa convencao evita duplicacao de logica entre `ProcessWebhookEventHandler.ExtractWebhookSecret` e os novos handlers de PUT/GET. (c) **Application-layer abstractions for provider-specific behavior**: a Slice 2-C demonstrou que e possivel ter `IProviderWebhookManagementClient` + `IProviderWebhookRegistrationFeaturePolicy` em `Application/Abstractions/Providers/` mesmo quando a implementacao e AbacatePay-specific. A implementacao concreta vive em `Infrastructure.Providers.AbacatePay/` e a feature flag segue a configuracao do provider concreto. **ANTI-PATTERN (DO NOT)**: importar `PaymentHub.Infrastructure.*` em `PaymentHub.Application` (quebra `scripts/agent-architecture-check.sh`). (d) **DTOs de webhook management NAO aceitam `tenantId`/`applicationId`** — re-asserting Slice 6-B. Endpoint autenticado server-to-server SEMPRE deriva esses ids de `ITenantContext` via try/catch em `InvalidOperationException` retornando 401. (e) **Campos configuracionais (callbackUrl, eventos, status) ficam em colunas own-column non-sensitive**. `webhookSecret` continua dentro de `EncryptedCredentials`. Regras de hash/criptografia + cleanup aplicados identicamente aos 2 paths (inbound webhook via `ProcessWebhookEventHandler.ExtractWebhookSecret`; outbound config via `ProviderAccountCredentialsInspector.HasWebhookSecret`).

### 2026-06-29 - Slice 3-IT descobre 2 producao bugs via E2E + adota WebApplicationFactory<Program> + CreateHost override para config eager

- Contexto: Ate o Slice 3-IT (2026-06-29), `tests/PaymentHub.IntegrationTests/` cobria apenas migrations + repositorios principais (10 testes Slice 1-IT) sem passar pelo pipeline HTTP real. P2-2 do phase board marcava "e2e API+Worker ainda nao coberto" desde 2026-06-17. O `Program.cs` do Api le `ConnectionStrings:Postgres` EAGERLY em `AddPaymentHubPostgres(builder.Configuration)` na linha 62, antes do host ser construido, o que impede `IWebHostBuilder.ConfigureAppConfiguration` (via `ConfigureWebHost`) de injetar overrides que cheguem a tempo. Alem disso, o `PaymentAttempt` era adicionado a `payment.Attempts` sem passar pelo DbSet, e o EF Core nao detectava o novo item via collection navigation (trackeava como Modified em vez de Added).

- Decisão: (1) **Migrar `webhook_events.raw_payload` de `jsonb` para `text`**: o Postgres `jsonb` normaliza o JSON no insert (insere um espaco apos cada `:`, `,` e quebra linhas de objetos), o que quebra HMAC sobre o body bruto. Como `WebhookEvent.RawPayloadJson` e usado apenas pelo verifier de HMAC + normalizer (nunca indexado/consultado no SQL), `text` e o tipo correto. Migracao nova `20260629205545_ChangeRawPayloadToText` aplicada via Testcontainers `MigrateAsync()`. (2) **Chamar `_payments.AddAttemptAsync(attempt, ct)` explicitamente** apos `payment.RegisterAttempt(...)` no `ProcessWebhookEventHandler.ProcessAsync`. A collection navigation de `Payment` (`IReadOnlyCollection<PaymentAttempt>` backed by `private readonly List`) nao e detectada de forma confiavel como `Added` pelo change tracker do EF Core 10 — em testes, o item aparece como `Modified`, e o UPDATE subsequente afeta 0 linhas (o novo Guid nao tem row correspondente), levantando `DbUpdateConcurrencyException`. O repositorio ja tinha `AddAttemptAsync` desde o Slice 1 (era usado pelo checkout handler); o slice 3-IT apenas conecta o webhook handler ao metodo correto. (3) **`WebApplicationFactory<Program>` exige `CreateHost(IHostBuilder)` override** (NAO apenas `ConfigureWebHost(IWebHostBuilder)`) para instalar um `IHostBuilder.ConfigureHostConfiguration` source com `ConnectionStrings:Postgres` + `PaymentHub:*` + `Providers:AbacatePay:*` + `Bootstrap:Enabled=false`. O source e wired no `ConfigurationManager` live do `WebApplicationBuilder` ANTES de `Program.cs:62` avaliar `GetConnectionString("Postgres")`. `ConfigureAppConfiguration` so roda em `Build()`, ou seja, tarde demais. (4) **Re-registrar HttpClient nomeado via `ConfigurePrimaryHttpMessageHandler`** em `ConfigureTestServices`: chamar `services.AddHttpClient("abacatepay").ConfigurePrimaryHttpMessageHandler(() => _abacateHandler)` APOS o `AddPaymentHubPostgres` registrar o client real faz o `HttpMessageHandlerBuilderActions` ser executado em ordem de registro, e o ultimo `PrimaryHandler` ganha — funciona porque `AddHttpClient` registra via `ConfigureOptions`, e a chamada subsequente adiciona um builder action que define `b.PrimaryHandler = fakeHandler`. (5) **`WebhookProcessorWorker` NAO roda dentro do `WebApplicationFactory`**; testes E2E invocam `IProcessWebhookEventHandler.ProcessAsync(webhookId, ct)` manualmente via `factory.Services.CreateScope()` apos o POST do webhook. Decisao documentada na spec de teste. (6) **`ResetDatabaseAsync` espelha `TRUNCATE ... RESTART IDENTITY CASCADE`** em ordem topologica reversa via `factory.CreateDbContext()` (nao via scope), garantindo isolamento entre testes. (7) **Por-teste `PaymentHubApiFactory`** evita cross-contamination dos fakes (`CallCount`, `_last`) entre testes; custo ~1-2s de boot por teste, aceitavel para 4 testes.

- Evidencia: 7 arquivos novos em `tests/PaymentHub.IntegrationTests/` (`Infrastructure/PaymentHubApiFactory.cs`, `Infrastructure/PostgresFixture.cs`, `Infrastructure/PostgresCollection.cs`, `Support/AbacatePayFakeHttpHandler.cs`, `Support/ApplicationWebhookCaptureHandler.cs`, `Support/E2ESeedHelpers.cs`, `EndToEnd/AbacatePayCheckoutE2ETests.cs` com 4 testes P1). 2 migracoes + 1 alteracao de config: `src/PaymentHub.Infrastructure.Postgres/Migrations/20260629205545_ChangeRawPayloadToText.cs`; `EntityConfigurations.cs` muda `webhook_events.raw_payload` para `text`; `src/PaymentHub.Application/Webhooks/WebhookHandlers.cs` ganha `_payments.AddAttemptAsync(attempt, ct)` em `ProcessAsync` com comentario inline explicando o EF tracking. 1 unit test ajustado: `ProcessWebhookEventHandlerAbacatePayTests.BuildCommonMocks()` adiciona setup default `.Setup(p => p.AddAttemptAsync(It.IsAny<PaymentAttempt>(), It.IsAny<CancellationToken>())).Returns(Task.CompletedTask)` para nao quebrar com `MockBehavior.Strict`. Suite previa: 418 testes. Suite nova: **422 testes (+4 E2E, +0 unit)**, todos passando. Build limpo (0 errors / 0 warnings em 9 projetos). `dotnet test PaymentHub.IntegrationTests.csproj` 14 passando (10 Slice 1-IT + 4 Slice 3-IT). `scripts/agent-architecture-check.sh` e `scripts/agent-docs-check.sh` passaram. `git diff --check` limpo. Relatorio em `docs/audits/slice-3-it-e2e-api-postgres-outbox-provider-report-2026-06-29.md`. Specs `007-inbox-outbox-workers.md` e `011-security-and-compliance.md` referencias.

- Impacto para proximos agentes: (a) **Postgres `jsonb` reformata JSON** (espaco apos `:`, virgula e quebra de linha) — sempre que armazenar corpo bruto que precisa ser validado por HMAC, assinatura digital, hash ou checksum externo, usar `text` (ou `json`, que preserva o original). `jsonb` so quando o payload precisa de queries SQL/GIN-index sobre seu conteudo. **ANTI-PATTERN (DO NOT)**: reverter `webhook_events.raw_payload` para `jsonb` em nova migration; o teste `ProviderWebhook_ValidSignature_UpdatesPaymentAndEnqueuesOutbox` quebra com HMAC `SignatureMismatch`. (b) **EF Core collection navigation nao trackeia confiavelmente** — quando voce adiciona um novo item via `entity.Navigation.Add(item)`, o change tracker pode classifica-lo como `Modified` em vez de `Added`. Para garantir `Added`, sempre chamar `repository.AddAsync(item, ct)` ou `dbContext.Set<T>().Add(item)` explicitamente. O `_attempts.Add(attempt)` em `Payment.RegisterAttempt` ja era buggy; o `AddRangeAsync(payment.Attempts)` no `PaymentRepository.AddAsync` (usado pelo checkout) mascara o problema porque roda junto. Webhook handler nao tinha essa protecao ate o Slice 3-IT aplicar `_payments.AddAttemptAsync(attempt, ct)`. **ANTI-PATTERN (DO NOT)**: chamar `payment.RegisterAttempt(...)` em outros handlers (refunds, chargebacks, expirations, etc.) sem parear com `await _payments.AddAttemptAsync(attempt, ct)`; o teste E2E correspondente quebra com `DbUpdateConcurrencyException`. **Ver tambem**: `feature_list.md` linhas `PH-PROVIDER-WEBHOOK-RAWPAYLOAD-TEXT` e `PH-PROVIDER-WEBHOOK-ATTEMPT-TRACKING` (marcadas `BLOCKER for Phase 7-IT`); secao "Anti-Regression Notes" no topo de `docs/audits/slice-3-it-e2e-api-postgres-outbox-provider-report-2026-06-29.md`. (c) **`WebApplicationFactory<Program>` + `WebApplication.CreateBuilder(args)`** exige `CreateHost(IHostBuilder)` override com `ConfigureHostConfiguration` (NAO apenas `ConfigureWebHost`) para config que `Program.cs` le eagerly. (d) **Para re-registrar um named `HttpClient`** ja configurado pela app, basta chamar `AddHttpClient(name).ConfigurePrimaryHttpMessageHandler(() => fake)` em `ConfigureTestServices` — o ultimo `PrimaryHandler` ganha. (e) **`WebhookProcessorWorker`/`OutboxDispatcherWorker` NAO sao hospedados por `WebApplicationFactory`**: invocar `IProcessWebhookEventHandler.ProcessAsync(webhookId, ct)` e `IOutboxPublisher.EnqueueAsync(...)` manualmente nos testes E2E. (f) **`StringContent` + `StreamReader` roundtrip preserva bytes UTF-8** (sem BOM) se e somente se a coluna for `text`/`varchar`; `jsonb` quebra a igualdade. Diagnostic dump mostrando `stored[0..40]={"id": "evt-...` vs `sent[0..40]={"id":"evt-...` e o sinal classico. (g) **Por-teste factory** mantem estado dos fakes isolado; reset do DB entre testes via `ResetDatabaseAsync` que faz `TRUNCATE ... RESTART IDENTITY CASCADE` em ordem topologica reversa. (h) **Mocks com `MockBehavior.Strict` quebram** quando nova chamada de producao e adicionada — `Mock<IPaymentRepository>` sem setup para `AddAttemptAsync` levanta no Slice 3-IT; preferir `MockBehavior.Loose` em helpers compartilhados OU adicionar setup default que retorna `Task.CompletedTask`.

### 2026-06-29 - Slice 2-B: AbacatePay webhooks externos usam verifier puro + normalizer puro + routing por metadata + fail-fast 401 no controller

- Contexto: Ate o Slice 2-B (2026-06-29), o `AbacatePayProviderAdapter.ParseWebhookAsync` ainda era o parsing JSON naive implementado para o Slice 2-A (fazia match de `data.id`/`data.status` e retornava `IsValid=true` sem verificar HMAC nem validar evento). O controller aceitava qualquer provider sem assinatura. O `ProcessWebhookEventHandler` carregava o adapter com `ProviderWebhookRequest(RawBody, Signature, Headers)` mas o adapter nao tinha `webhookSecret` para validar. O gap era: HMAC de webhooks externos AbacatePay nunca era verificado, e o roteamento para o `ProviderAccount` correto dependeria de um "tenant sweep" perigoso se tentado sem metadata no payload.

- Decisão: (1) **Verifier puro e normalizer puro separados** em `src/PaymentHub.Infrastructure.Providers/AbacatePay/Webhooks/`. `IAbacatePayWebhookSignatureVerifier.Verify(rawBody, signatureHeader, webhookSecret)` retorna `AbacatePayWebhookSignatureFailure` (`None`/`MissingSignature`/`MalformedSignature`/`MissingSecret`/`SignatureMismatch`) sem lancar exceptions. `IAbacatePayWebhookNormalizer.Normalize(rawBody)` desserializa `AbacatePayWebhookEnvelope` (campos `id`/`event`/`apiVersion`/`devMode`/`data`) e suporta apenas eventos `transparent.completed|refunded|disputed|lost`. Verifier usa `CryptographicOperations.FixedTimeEquals` em `byte[] expected` vs `byte[] provided` (comprimento igualado explicitamente antes para evitar panic quando Base64 muito curto). Normalizer mapeia `eventType` para `PaymentStatus` via `MapEvent` (decisoes documentadas em teste). (2) **`ProviderWebhookRequest` estendido** com `ProviderAccountId` e `WebhookSecret` init-only (backward-compatible — Fake/Stripe/MercadoPago continuam compilando e funcionando com `WebhookSecret=null`). A interface `ProviderWebhookParseResult` NAO foi alterada; o adapter AbacatePay traduz as categorias do verifier/normalizer para `ErrorMessage` seguro. (3) **Roteamento por metadata do payload**: `ProcessWebhookEventHandler.ResolveAbacatePayWebhookSecretAsync` faz parse permissivo de `data.metadata.{tenantId, applicationId, paymentId}` (tolerante a JSON malformado, devolve `null` em vez de lancar) e usa `IPaymentRepository.GetByIdForTenantAsync(tenantId, paymentId)` para localizar o pagamento no escopo do tenant. Quando metadata ou pagamento nao existem, NAO varre tenants e NAO chama o adapter — marca o `WebhookEvent` como `Failed` com `LastError` categorizado. (4) **`webhookSecret` preferido, `secret` legacy aceito**: o handler extrai `credentials.webhookSecret` (campo explicito) e cai para `credentials.secret` (formato legado da Slice 2-A) — nenhum dos dois e logado. `ICredentialProtector.Unprotect` e chamado uma unica vez; o JSON desprotegido e descartado apos a extracao. (5) **Fail-fast no controller antes de persistir**: `ProviderWebhooksController.Receive` aceita `X-Webhook-Signature` (canonical) e `X-Provider-Signature` (legacy fallback). Quando `providerCode == "AbacatePay"` (case-insensitive) e a assinatura esta ausente/branco, retorna `401 Unauthorized { error = "missing_signature" }` **sem** gravar `WebhookEvent` nem chamar o handler. Outros providers preservam o comportamento legacy. Quando ambos os headers chegam, `X-Webhook-Signature` tem prioridade. (6) **Defensive sanitization**: `WebhookEvent.LastError` passa por `ProcessWebhookEventHandler.Sanitize(...)` que remove `\r`/`\n`/`\0` e limita a 2000 chars — `ex.Message` jamais e persistido se contiver stack traces ou bodies HTTP. (7) **Bug fix retroativo no Slice 2-B.1**: `HmacAbacatePayWebhookSignatureVerifier.Verify` trocou `IsNullOrEmpty` por `IsNullOrWhiteSpace` em ambos os checks (signature e secret). Whitespace como `"   "` vazava como `SignatureMismatch` em vez de `MissingSignature`/`MissingSecret`. (8) **Bug fix em teste auto-inconsistente**: `ParseWebhookAsync_ShouldReturnInvalid_WhenSignatureDoesNotMatch` construia o request com `WebhookSecret = "totally-different-secret"` E assinava com o mesmo segredo, o que fazia a verificacao passar. Corrigido para assinar com `signing-secret-a` e passar `WebhookSecret = "signing-secret-b"`.

- Evidência: 9 arquivos produtivos criados/alterados em `src/PaymentHub.Infrastructure.Providers/AbacatePay/Webhooks/` (verifier, normalizer, signature failure enum, normalization result, interface) + `Models/AbacatePayWebhookEnvelope.cs` + `Models/AbacatePayTransparentWebhookData.cs`. `ProviderModels.cs` ganha `ProviderAccountId`/`WebhookSecret` init-only. `AbacatePayProviderAdapter.ParseWebhookAsync` reescrito (5 paths de erro). `WebhookHandlers.cs` ganha `ResolveAbacatePayWebhookSecretAsync`, `TryReadAbacatePayMetadata`, `ExtractWebhookSecret`, `Sanitize`. `ProviderWebhooksController.cs` ganha `[FromHeader(Name = HmacAbacatePayWebhookSignatureVerifier.SignatureHeaderName)]` e fail-fast. Suite previa: 348 testes. Suite nova: **418 testes** (+70). Verificado: `dotnet test --filter "FullyQualifiedName~AbacatePay"` 125 passing; `--filter "FullyQualifiedName~Webhook"` 193 passing; `--filter "FullyQualifiedName~Provider"` 135 passing; suite completa 418 passing. `scripts/agent-architecture-check.sh` passou; `git diff --check` limpo. Relatorio em `docs/audits/slice-2b-abacatepay-webhooks-report-2026-06-29.md`. Specs `006`/`008`/`011` atualizadas.

- Impacto para proximos agentes: (a) Ao adicionar **novo provider real** que exige HMAC no webhook, copiar o pattern: criar `I<Provider>WebhookSignatureVerifier` + `I<Provider>WebhookNormalizer` puros (sem excecoes) em `src/PaymentHub.Infrastructure.Providers/<Provider>/Webhooks/`. Manter `ProviderWebhookRequest` backward-compatible (init-only). (b) Ao adicionar **novo evento** ao normalizer AbacatePay (ex.: Boleto transparente, Pix out), seguir o mesmo pattern: evento suportado na whitelist `SupportedEvents`, decisao de mapeamento documentada em `MapEvent` test. Nao confiar em `TransparentData` para carregar `webhookSecret` (campo do envelope nunca traz segredo). (c) **NUNCA** persistir `webhookSecret` em `WebhookEvent`. Adicionar coluna a tabela e' proibido pela politica de seguranca (vide `docs/specs/011-security-and-compliance.md`). (d) **NUNCA** varrer todos os tenants para resolver `ProviderAccount`. Usar metadata do payload primeiro (`data.metadata.*` ou equivalente do novo provider); em fallback, confiar em `providerPaymentId -> Payment -> TenantId/ApplicationId -> ProviderAccount`. (e) **NUNCA** trocar `IsNullOrEmpty` por `IsNullOrWhiteSpace` em verificacoes de segredo/assinatura **sem** ajustar testes — Slice 2-B.1 ja ensinou a licao. (f) Ao expandir o `ProviderWebhooksController` para outro provider que exige HMAC, replicar o fail-fast com header canonico (ex.: `Stripe-Signature` para Stripe, `X-Notification-Signature` para MercadoPago) e adicionar o providerCode a lista branca do fail-fast. (g) **`ProviderWebhookParseResult.IsValid=false`** sempre carrega `ErrorMessage` seguro (apenas categoria). Mensagens com detalhes de stack/body devem permanecer em log estruturado, nunca no banco. (h) **Metadata path no payload**: AbacatePay v2 envia `data.metadata.{tenantId, applicationId, paymentId}` em Checkout Transparente PIX. Proximo provider que replicar a slice deve usar o mesmo pattern (HTTPS obrigatorio para criar webhooks via API, idempotency por `provider_event_id`). (i) **Multi-instancia**: o handler atual NAO usa `FOR UPDATE SKIP LOCKED` para o `WebhookEvent` que vai processar — segue o mesmo modelo single-instance do OutboxDispatcherWorker. Slice 7-IT (multi-instancia) vai precisar de sweep automatico de `Processing` orfaos tambem para `WebhookEvent`.

### 2026-06-26 - Slice 1-IT base de testes de integracao usa Testcontainers + collection fixture + TRUNCATE CASCADE + DI manual

- Contexto: Ate o Slice 1-IT (2026-06-26), `tests/PaymentHub.IntegrationTests/` era casca vazia (`PaymentHub.IntegrationTests.csproj` + `bin/obj` de tentativa anterior, zero codigo). P2-2 do phase board registrava "projeto de testes de integracao sem testes descobertos" desde 2026-06-17. CI nao tem Docker obrigatorio; suite unitaria precisa continuar rapida. Constraint: nao tocar provider real, nao tocar arquitetura.
- Decisao: (1) **Testcontainers.PostgreSql 4.12.0** (target `net8.0`, compativel com .NET 10) com imagem `postgres:16-alpine`; sobe 1 container real por run e aplica migrations via `_db.Database.MigrateAsync()` no `InitializeAsync` do `IAsyncLifetime`. (2) **`[CollectionDefinition("Postgres")]` + `ICollectionFixture<PostgresFixture>`** compartilha 1 container por run inteira (~10-30s de startup); alternativas (database-per-test, schema-per-test) descartadas por lentidao ou complexidade. (3) **Opt-in via `[Trait("Category", "Integration")]`**: `dotnet test PaymentHub.slnx` nao roda integracao por padrao; CI sem Docker nao quebra. Suite rapida: `dotnet test --filter "Category!=Integration"`. Suite completa: `dotnet test tests/PaymentHub.IntegrationTests/PaymentHub.IntegrationTests.csproj`. (4) **`ResetDatabaseAsync()` usa `TRUNCATE ... RESTART IDENTITY CASCADE` em ordem topologica reversa** (filhos antes de pais: `payment_attempts, payments, webhook_events, outbox_events, idempotency_keys, api_keys, provider_accounts, audit_logs, application_clients, tenants`). `CASCADE` e defensivo contra FKs imprevistas; `RESTART IDENTITY` zera sequences (PKs sao Guid mas o comando nao faz mal). (5) **DI manual minimo** em `IntegrationTestFactory` (nao chama `AddPaymentHubPostgres`): `ServiceCollection` + registros escopados de `PaymentHubDbContext`, repos, `IOutboxPublisher`/`IOutboxRepository`/`IOutboxEventStore`, `IWebhookSecretProtector`/`ICredentialProtector`/`IApiKeyHasher`/`IWebhookSigner`/`IIdempotencyRequestHasher`/`IClock`. Justificativa: `AddPaymentHubPostgres` puxa `IHttpClientFactory` + `HttpApplicationWebhookDispatcher` que nao sao exercitados pelo slice; reduzir superficie de bootstrap reduz tempo de teste e elimina `IHttpClientFactory` no host de testes. (6) **`MigrationsAssembly(typeof(PaymentHubDbContext).Assembly.GetName().Name)`** explicito no `DbContextOptionsBuilder.UseNpgsql(...)` — garante que EF Core procura migrations em `PaymentHub.Infrastructure.Postgres`, nao no assembly de chamada. (7) **Chaves de cifra com >= 32 bytes literais** (`integration-test-webhook-secret-key-32+chars!` etc.) embora os protectors aceitem mais curtas via `PadRight(32, '0')`. Defensivo contra qualquer dependencia futura de tamanho minimo. (8) **`await using` somente em `DbContext`/`DbCommand`/`DbDataReader`** (todos implementam `IAsyncDisposable`); **`using` sincrono em `IServiceScope`** (NAO implementa `IAsyncDisposable` — gotcha do .NET). Fixture e testes foram revistos manualmente linha por linha para garantir que `await using var scope = _factory.CreateScope()` nao persiste (CS8417 quebra build). (9) **Helpers de seed idempotentes por ID fixo opcional**: `SeedTenantAsync(id: Guid.NewGuid())`, `SeedApplicationClientAsync(tenantId, id: Guid.NewGuid())` etc. — testes podem passar ids explicitos para asserir roundtrip, ou deixar o helper gerar Guid. (10) **Gap de `Processing` orfao documentado inline** em `OutboxPendingQueryTests.cs` (comentario explicito + assert `Should().NotContain(processing.Id, because: "the query only returns Pending; orphan Processing is a known gap")`). Sweep automatico + `FOR UPDATE SKIP LOCKED` sao escopo de slice proprio (Phase 7 multi-instancia), ja documentados em ADR-0010.
- Evidencia: 10 testes novos em `tests/PaymentHub.IntegrationTests/` (1 migrations + 2 tenant/application + 2 webhook secret + 2 provider account + 2 outbox persistence + 1 outbox pending query). Suite previa: 281 testes. Suite nova: **291 testes** (+10). Build limpo (0 errors / 0 warnings em 9 projetos). `dotnet test tests/PaymentHub.IntegrationTests/PaymentHub.IntegrationTests.csproj` em 7.6-32s (inclui startup do container). `scripts/agent-architecture-check.sh` e `scripts/agent-docs-check.sh` passaram. Relatorio em `docs/audits/slice-1-it-postgres-integration-tests-report-2026-06-26.md`. TRX com a lista nominal dos 10 testes disponivel.
- Impacto para proximos agentes: (a) Ao adicionar testes de integracao, copiar o padrao: `[Collection(PostgresCollection.Name)]` + `[Trait("Category", "Integration")]` + ctor que recebe `PostgresFixture` + chama `new IntegrationTestFactory(fixture)`. NUNCA chamar `AddPaymentHubPostgres` direto na fixture. (b) Ao adicionar nova tabela na migration, atualizar o `expectedTables` em `MigrationSmokeTests` e a ordem em `ResetDatabaseAsync` (topologica reversa). (c) Ao adicionar novo teste que precisa de DI escopado, usar `using var scope = _factory.CreateScope()` (sincrono) e injetar o servico via `scope.ServiceProvider.GetRequiredService<T>()`. NUNCA `await using var scope`. (d) Ao adicionar novo teste que precisa de `DbContext`/`DbCommand`/`DbDataReader`, usar `await using` (assincrono). (e) CI sem Docker: usar `--filter "Category!=Integration"` ou simplesmente rodar `tests/PaymentHub.UnitTests/`. (f) CI com Docker: rodar `tests/PaymentHub.IntegrationTests/` antes de `tests/PaymentHub.UnitTests/` para falhar rapido em caso de migration quebrada. (g) Ao evoluir para testes end-to-end com `WebApplicationFactory` (Slice 3-IT), reutilizar `PostgresFixture` mas construir a API via `WebApplicationFactory<Program>` apontando para `fixture.ConnectionString`. (h) Ao migrar para multi-instancia (Phase 7), adicionar `FOR UPDATE SKIP LOCKED` em `OutboxRepository.GetPendingForDispatchAsync` e `OutboxRepository.AddAsync` dentro de transacao. A fixture ja permite testar isso adicionando dois `DbContext` em paralelo no mesmo scope.

### 2026-06-26 - Dispatcher HTTP real do Outbox vive em Infrastructure.Postgres.Webhooks com tenant guard, IOutboxEventStore, IClock, LastError seguro e fail-fast no startup

- Contexto: Ate o Slice 7-A.3 (2026-06-26), `PaymentHub.Worker/Program.cs` registrava um `internal sealed class NoopApplicationWebhookDispatcher` inline (linhas 62-70 do Program.cs antigo). O ciclo de Outbox era teoricamente correto (eventos lidos, marcados como `Sent`, retry policy aplicada), mas nenhum HTTP request real era feito. Gap P1-4 da auditoria de 2026-06-17 ficou exposto por toda a Phase 7. Alem disso, o dispatcher lia `ApplicationClient.WebhookSecret` raw para assinar HMAC (Slice 6-C ja tinha protegido o segredo, mas o dispatcher ainda fazia parte do codigo pre-Slice-6-C).
- Decisão: (1) `HttpApplicationWebhookDispatcher` realocado para `src/PaymentHub.Infrastructure.Postgres/Webhooks/HttpApplicationWebhookDispatcher.cs` com namespace `PaymentHub.Infrastructure.Postgres.Webhooks`. Worker **nao** pode depender de Api (validado por `scripts/agent-architecture-check.sh`); o dispatcher tem que acessar `IApplicationClientRepository`, `IOutboxEventStore`, `IWebhookSecretProtector`, `IOptions<PaymentHubOptions>` e fazer HTTP — tudo isso ja vive em `Infrastructure.Postgres`. (2) Lifetime **Scoped** (registrado em `AddPaymentHubPostgres`): o dispatcher depende de `IApplicationClientRepository` (Scoped) e de `IOptions<PaymentHubOptions>` (Singleton); Singleton seria problematico por causa do repositorio. (3) `HttpClient` via `IHttpClientFactory.CreateClient("application-webhook")` (registrado em `AddPaymentHubPostgres`). `HttpClient` direto causa socket exhaustion; o factory gerencia o pool corretamente. (4) `IOutboxEventStore` em `Application/Abstractions/Outbox/IOutboxEventStore.cs` com metodo `Task SaveAsync(OutboxEvent, CancellationToken)` e implementacao `EfOutboxEventStore` em `Infrastructure.Postgres/Outbox/`. Permite testar `OutboxDispatcherWorker` sem `DbContext` direto. (5) `OutboxDispatcherWorker` injeta `IOutboxRepository`, `IOutboxEventStore` e `IClock` (este ja existia). `DateTime.UtcNow` removido em favor de `_clock.UtcNow` para retry policy deterministica em testes. (6) Tenant guard via `_apps.GetByTenantAndIdAsync(outboxEvent.TenantId, outboxEvent.ApplicationId, ct)` em vez de `_apps.GetByIdAsync(applicationId, ct)` — o `OutboxEvent` ja traz `tenant_id` separado, entao usar `GetByIdAsync` permitiria cross-tenant access se `application_id` fosse ambiguo. (7) `OutboxEvent.LastError` agora armazena apenas `WebhookDispatcherCategory` (enum de 7 valores: `HttpFailure`, `NetworkError`, `Timeout`, `UnprotectFailure`, `MissingWebhookUrl`, `MissingWebhookSecret`, `UnexpectedDispatcherError`) + `int?` statusCode. `ex.Message` nunca e persistido. `MarkRetryWithStatus(category, statusCode, nextRetryAt)` e `MarkFailedWithStatus(category, statusCode)` sao os unicos metodos publicos para atualizar `LastError`. `WebHookDispatcherException` carrega a categoria + statusCode e sua mensagem e generica por design. (8) `UnprotectFailure` nao envia HTTP request — "abortar cedo" em vez de "enviar sem assinatura". `MissingWebhookUrl` marca como `Failed` direto sem retry. (9) `WebhookUrl` validada por `RegisterApplicationClientValidator` com `WebhookUrlValidator` helper puro (`internal static class`, `IsAllowed(string?, bool, out string?)`) — HTTPS obrigatorio, HTTP apenas em Development para loopback, bloqueio de `localhost`/`*.localhost`/`*.local`, loopback IPv4/IPv6/`::ffff:127.0.0.1`, RFC1918 (`10/8`, `172.16/12`, `192.168/16`), link-local/IMDS (`169.254/16`, `fe80::/10`), unspecified, broadcast. Mensagem de erro unificada anti-enumeration. (10) Worker tem fail-fast de `IWebhookSecretProtector` no startup (`Worker/Program.cs:53-56`) em scope anonimo antes de `host.Run()` — se `PaymentHub:WebhookSecretEncryptionKey` ausente, falha cedo com `InvalidOperationException` em vez de esperar o primeiro dispatch.
- Evidencia: 9 sub-slices (7-A.1 a 7-A.9) implementados entre 2026-06-25 e 2026-06-26. Suite previa: 178 testes. Suite nova: **281 testes** (+103). ADRs `ADR-0007-webhook-secret-protection.md` e `ADR-0010-real-outbox-dispatcher-location.md` consolidadas. Specs `007-inbox-outbox-workers.md` e `011-security-and-compliance.md` atualizadas. Reports em `docs/audits/slice-7a-real-outbox-dispatcher-report-2026-06-26.md` (consolidado), `slice-7a5-webhook-url-ssrf-report-2026-06-26.md` (SSRF) e `slice-7a6-worker-appsettings-webhook-secret-key-report-2026-06-26.md` (configuracao). Build limpo, `scripts/agent-architecture-check.sh` passou, `git diff --check` limpo. Phase 7 alcancou 0 gaps P1 proprios em 2026-06-26.
- Impacto para proximos agentes: (a) Ao adicionar novo consumer HTTP para `OutboxEvent` (ex.: webhook de retry para parceiros, dispatch para sistema externo), copiar o padrao: dispatcher em `Infrastructure.Postgres.Webhooks/` (ou `Infrastructure.Postgres.<Dominio>/`), lifetime Scoped, `IHttpClientFactory` nomeado, DI centralizado em `AddPaymentHubPostgres`, tenant guard via `GetByTenantAndIdAsync`, `IOutboxEventStore` para testabilidade. (b) Ao adicionar nova categoria de erro ao dispatcher, estender `WebhookDispatcherCategory` e atualizar testes — nunca persistir `ex.Message`. (c) Ao adicionar novo tipo de URL que sai do Payment Hub para consumidores externos (webhook, redirect URL, postback), replicar a validacao HTTPS/SSRF usando `WebhookUrlValidator` (ou extrair helper equivalente para o novo dominio). (d) Ao adicionar nova chave criptografica obrigatoria em producao, replicar o fail-fast no startup: resolver o protector em scope anonimo antes de `host.Run()`, com placeholder vazio em `appsettings.json` e valor fake em `appsettings.Development.json`. (e) Helpers puros `internal static` + `InternalsVisibleTo("PaymentHub.UnitTests")` no `.csproj` continuam sendo o caminho para testabilidade sem inflar a API publica (ja usado em `WebhookUrlValidator`; ver entrada de 2026-06-26 do Slice 7-A.5). (f) Ao migrar para multi-instancia (Phase 7 multi-instance, fora do MVP), adicionar `FOR UPDATE SKIP LOCKED` em `OutboxRepository` e sweep automatico de eventos `Processing` orfaos. Documentado em `docs/specs/007-inbox-outbox-workers.md` e ADR-0010.

### 2026-06-25 - Webhook secret protection usa AES reversivel com prefixo de proposito, parametrizacao explicita e chave separada de credenciais

- Contexto: O Slice 6-C enderecou o gap P1-5 (`ApplicationClient.WebhookSecret` persistido em texto claro). O segredo precisa ser recuperado em memoria para assinar HMAC de webhooks internos, entao hash unidirecional nao serve. A coluna ja existia (`varchar(500) nullable`) e nenhum dado produtivo estava persistido, entao migration estrutural nao era justificada. Credenciais de provider ja usavam `ICredentialProtector`/`AesCredentialProtector` com chave separada (`PaymentHub:CredentialEncryptionKey`), entao o padrao do projeto para "segredo reversivel em repouso" ja estava definido.
- Decisão: (1) Interface `IWebhookSecretProtector` em `PaymentHub.Application/Abstractions/Security/ICrypto.cs` com `Protect`/`Unprotect` — mesma familia de `ICredentialProtector`. (2) Implementacao `AesWebhookSecretProtector` em `Infrastructure.Postgres/Security/CryptoServices.cs` com AES-CBC + IV randomico de 16 bytes + prefixo de proposito `PaymentHub.ApplicationClient.WebhookSecret.v1` verificado em tempo constante via `CryptographicOperations.FixedTimeEquals`. (3) Parametro nomeado `protectedWebhookSecret` no construtor e em `UpdateWebhook(...)` da entidade `ApplicationClient` — o dominio nao ve raw, so blob ja protegido. (4) Chave separada: `PaymentHub:WebhookSecretEncryptionKey` (independente de `CredentialEncryptionKey`) para que a rotacao tenha blast radius menor. (5) Sem `IDataProtectionProvider` (Data Protection do ASP.NET Core) — o projeto ja optou por AES proprio em `AesCredentialProtector`, e Data Protection exigiria injecao de dependencia web-framework-specific no `PaymentHub.Application`, quebrando Clean Architecture. (6) `HttpApplicationWebhookDispatcher.DispatchAsync` chama `Unprotect` imediatamente antes de `Sign`; se falhar, loga e **nao** envia HTTP request — preferencia explicita por "abortar cedo" em vez de "enviar sem assinatura".
- Evidencia: `src/PaymentHub.Application/Abstractions/Security/ICrypto.cs:15`, `src/PaymentHub.Infrastructure.Postgres/Security/CryptoServices.cs:80-150`, `src/PaymentHub.Domain/Entities/ApplicationClient.cs:33-58`, `src/PaymentHub.Api/Webhooks/HttpApplicationWebhookDispatcher.cs:52-67`. 27 testes novos (`AesWebhookSecretProtectorTests`, `RegisterApplicationClientHandlerTests`, `DevelopmentDataSeederTests`, `HttpApplicationWebhookDispatcherTests`). Build limpo, 133 testes passando. Relatorio em `docs/audits/slice-6c-webhook-secret-protection-report-2026-06-25.md`.
- Impacto para proximos agentes: Ao proteger um segredo reversivel em repouso no projeto, copiar o padrao: `I<Suffix>Protector` em `Application.Abstractions.Security`, implementacao `Aes<Suffix>Protector` em `Infrastructure.Postgres.Security`, chave separada em `PaymentHubOptions`, registro singleton em `PostgresServiceCollectionExtensions`, parametro nomeado `protected*` na entidade, log de erro sem expor valor. Prefixar o payload com proposito (`<Purpose>.v<N>`) e verificar via `CryptographicOperations.FixedTimeEquals` para rejeitar blobs de outros sistemas ou versoes anteriores. Nao usar `IDataProtectionProvider` para manter consistencia com `ICredentialProtector`. Em testes, usar `FakeWebhookSecretProtector` com marker previsivel para que `persisted.X.StartsWith(marker)` seja assercao suficiente. Se o consumidor precisa do raw em runtime, o caminho "abortar cedo quando `Unprotect` falha" eh preferivel a "enviar sem assinatura" — o dispatcher do Slice 6-C exemplifica.

### 2026-06-24 - OpenCode harness deve separar config estrita, agentes curtos e skills sob demanda

- Contexto: A evolucao do harness OpenCode precisava adicionar agentes primarios/subagents, skills locais, permissoes seguras e scripts de verificacao sem quebrar o schema estrito do OpenCode nem inflar `AGENTS.md`.
- Decisão: Manter `.opencode/opencode.json` apenas estrutural (`skills`, `permission`, `watcher`, `tool_output`, `compaction` etc.), usar `.opencode/agents/*.md` como fonte de verdade de comportamento/metadados/permissoes dos agentes, mover fluxos para `docs/harness/opencode.md` e `agent-operating-model.md`, e criar skills pequenas em `.opencode/skills/*/SKILL.md`.
- Evidência: `scripts/agent-docs-check.sh` valida agentes, skills, ausencia de `agents`/`notes` top-level, ausencia de duplicacao via `agent`/`prompt` no JSON, reviewers sem edicao/subagents e `implementer` sem `edit: '*': allow`; `scripts/agent-architecture-check.sh` valida limites de camadas; `scripts/agent-smoke.sh` combina checks locais sem exigir provider real.
- Impacto para próximos agentes: Ao evoluir OpenCode, consulte `https://opencode.ai/config.json` antes de adicionar chaves, nao duplique agentes no JSON, mantenha reviewers com `edit: deny` e `task: deny`, registre skills no indice e reinicie o OpenCode apos mudar config/agentes/skills.

### 2026-06-23 - Agent-readiness funciona melhor com contexto progressivo e verificacao mecanica

- Contexto: A configuracao de Copilot/Codex precisava cobrir auditoria, instrucoes, prompts, agentes, skills, docs de uso, estado e verificacao sem transformar `copilot-instructions.md` em uma enciclopedia.
- Decisão: Manter `AGENTS.md` como indice operacional, `.github/copilot-instructions.md` curto, regras especificas em `.github/instructions/`, rotinas em `.github/prompts/` e `.github/skills/`, personas em `.github/agents/`, governanca em `docs/ai/`, estado em `feature_list.md`/`agent-progress.md` e verificacao em `scripts/agent-verify.sh`.
- Evidência: `scripts/agent-verify.sh` validou arquivos, frontmatter, ausencia de `.env` real e `docker compose config`; `dotnet restore`, `dotnet build` e `dotnet test` passaram, com 106 testes unitarios e projeto de integracao ainda sem testes descobertos.
- Impacto para próximos agentes: Ao adicionar novas regras para agentes, prefira contexto progressivo e enforcement por script/check/CI. Nao inflar `copilot-instructions.md`; coloque regras por area em `.github/instructions/` e processos repetiveis em prompts ou skills. Se novas lacunas de validacao surgirem, atualize `docs/ai/validation-checklist.md` e `scripts/agent-verify.sh`.

### 2026-06-18 - Bootstrap deve ser policy-driven, opt-in e nunca loggar credenciais

- Contexto: Antes do Slice 6-D, nao havia codigo de seed automatico e nenhuma politica explicita para criacao de tenant/application inicial. O codigo tinha `TenantsController` e `ApplicationsController` que o middleware bloqueava (requeriam API Key), criando um deadlock operacional. A auditoria apontou isso como gap P1-3.
- Decisão: Centralizar a politica em `IBootstrapPolicy` (Application) + `BootstrapOptions` (strongly-typed) + `HostBootstrapPolicy` (Api, le `IHostEnvironment`); seedor `IDevelopmentDataSeeder` cria apenas `tenant` + `application` (nunca API Key, provider account, secret ou credencial). Defaults de `appsettings.json` sao `Enabled=false`, `SeedDevelopmentData=false`, `AllowProductionBootstrap=false` (fail-safe). `appsettings.Development.json` liga `Enabled=true` e `SeedDevelopmentData=true` mas mantem `AllowProductionBootstrap=false`. Idempotencia via `ITenantRepository.GetBySlugAsync` e `IApplicationClientRepository.GetByTenantAndNameAsync` (novos metodos adicionados no slice). `Production` exige opt-in explicito para criar qualquer dado.
- Evidência: 12 testes em `tests/PaymentHub.UnitTests/Api/HostBootstrapPolicyTests.cs` e 9 testes em `tests/PaymentHub.UnitTests/Application/DevelopmentDataSeederTests.cs`. Build limpo, 106 testes passando. Relatorio em `docs/audits/slice-6d-bootstrap-admin-seed-policy-report-2026-06-18.md`.
- Impacto para próximos agentes: Sempre que adicionar um caminho automatico de criacao de dados iniciais (seed, migration data, fixture), passar por `IBootstrapPolicy`. Nao criar API Key, secret ou credencial via seedor — primeiro porque logging seguro e obrigatorio (regra de `docs/harness/security.md`), segundo porque a exibicao one-time de API Key depende do response do endpoint HTTP. Para resolver o deadlock residual de "como obter a primeira API Key em producao", alinhar com o painel admin (Phase 5) ou um canal externo documentado; nao introduzir bypass de `ApiKeyAuthenticationMiddleware`. Ao auditar `appsettings*.json`, conferir que a secao `Bootstrap` tem defaults seguros.

### 2026-06-18 - Em projetos .NET 10 com `Microsoft.NET.Sdk`, `IOptions<>` exige PackageReference explicita

- Contexto: Ao adicionar `IDevelopmentDataSeeder` em `PaymentHub.Application` (que usa `Microsoft.NET.Sdk` puro, nao `Microsoft.NET.Sdk.Web`), a primeira compilacao falhou em `IOptions<BootstrapOptions>` e `ILogger<>` porque o shared framework `Microsoft.AspNetCore.App` nao e transitive em projetos nao-Web. Os projetos `PaymentHub.Infrastructure.Postgres` e `PaymentHub.Api` ja tinham `IOptions<>` indiretamente (via SDK Web + providers) e funcionavam.
- Decisão: Adicionar `Microsoft.Extensions.Logging.Abstractions` 10.0.0 e `Microsoft.Extensions.Options` 10.0.0 como `PackageReference` no `PaymentHub.Application.csproj`. Manter as versoes alinhadas com a versao do .NET (10.0.x) para evitar conflitos de runtime.
- Evidência: Build do `PaymentHub.Application` falhou com `error CS0246: The type or namespace name 'IOptions<>' could not be found` ate a adicao dos packages; apos, build limpo.
- Impacto para próximos agentes: Ao adicionar uma classe Application que dependa de `IOptions<T>`, `ILogger<T>` ou `IHostEnvironment` (apesar deste ultimo exigir Microsoft.Extensions.Hosting), adicionar explicitamente os packages `Microsoft.Extensions.Options` e/ou `Microsoft.Extensions.Logging.Abstractions` no `.csproj` Application. Nao assumir que o shared framework Web cobre. A mesma logica vale para qualquer projeto que use `Microsoft.NET.Sdk` puro (bibliotecas).

### 2026-06-17 - Enforcement de status ativo deve acontecer no middleware, nao no handler

- Contexto: Gap P1-1 da auditoria de 2026-06-17 apontava que `Tenant`/`ApplicationClient` inativos continuavam consumindo a API quando a API Key estava `Active`. A spec `002-multitenancy-and-authentication.md` exige "Tenant suspenso/desativado ou application inativa deve impedir criacao de checkout".
- Decisão: Centralizar o enforcement em `ApiKeyAuthenticationMiddleware` (ponto de entrada de toda requisicao autenticada). Middleware consulta `ITenantRepository.GetByIdAsync` e `IApplicationClientRepository.GetByTenantAndIdAsync` apos validar hash + escopo da API Key, e retorna `403 Forbidden` quando `Status != Active`. Tenant/application inexistentes continuam retornando `401 Unauthorized` para nao vazar existencia. Handlers de aplicacao (ex.: `CreateCheckoutHandler`) permanecem presumindo `ITenantContext` valido.
- Evidência: 11 testes em `tests/PaymentHub.UnitTests/Api/ApiKeyAuthenticationMiddlewareTests.cs` cobrindo os 7 cenarios do slice + 4 cenarios correlatos (tenant/application inexistente, anti-leak 401, anti-leak 403). Build limpo e 70 testes passando. Relatorio em `docs/audits/slice-6a-active-status-enforcement-report-2026-06-17.md`.
- Impacto para próximos agentes: Nao duplicar verificacao de status em `CreateCheckoutHandler` ou outros handlers — o middleware ja garante. Para adicionar novos endpoints autenticados, manter o middleware como unico gate. Ao introduzir novos status (ex.: `Provisioning`, `Blocked`), estender a verificacao no middleware em vez de espalhar por handlers. Specs `002`, `011` e o report do slice sao referencia obrigatoria antes de mexer no `ApiKeyAuthenticationMiddleware`.

### 2026-06-17 - Testes de middleware exigem MemoryStream explicito em `DefaultHttpContext`

- Contexto: Ao adicionar testes que validam o body JSON de `WriteAsJsonAsync` em `DefaultHttpContext`, a primeira execucao falhou com `JsonReaderException: The input does not contain any JSON tokens` mesmo com `ctx.Response.Body.Position = 0`. Investigacao mostrou que `DefaultHttpContext.Response.Body` vem como `Stream.Null`, descartando todo o write.
- Decisão: Setar `ctx.Response.Body = new MemoryStream()` em qualquer helper de teste que use `DefaultHttpContext` e precise ler a resposta. Tambem nao comparar `ContentType` exato porque ASP.NET Core 10 adiciona `; charset=utf-8`; preferir `Should().StartWith("application/json")`.
- Evidência: Testes `RejectionResponses_ShouldNotLeakApiKeyOrEntityStatus` e `UnauthorizedResponses_ShouldNotLeakApiKeyOrEntityStatus` em `ApiKeyAuthenticationMiddlewareTests.cs` so passaram apos o ajuste.
- Impacto para próximos agentes: Antes de adicionar testes que inspecionam `ctx.Response.Body` ou `ContentType`, garantir `ctx.Response.Body = new MemoryStream()` no setup. Para comparar tipos MIME, usar `StartWith` em vez de `Be`.

### 2026-06-17 - `ApplicationClient` precisa de `Suspend()`/`Activate()` para paridade com `Tenant`

- Contexto: A auditoria documentou 3 valores para `ApplicationStatus` (`Active`, `Suspended`, `Disabled`), assim como em `TenantStatus`. Porem, so `Tenant` tinha `Suspend()`/`Activate()`; `ApplicationClient` nao. Para testar enforcement sem reflexao, era necessario um caminho publico para alterar `Status`.
- Decisão: Adicionar `ApplicationClient.Suspend()` e `ApplicationClient.Activate()` no dominio, mantendo `Status` como `private set`. Mudanca puramente comportamental, sem migration.
- Evidência: `src/PaymentHub.Domain/Entities/ApplicationClient.cs:46-58` (metodos adicionados). Testes em `ApiKeyAuthenticationMiddlewareTests.cs` instanciam `ApplicationClient` e chamam `Suspend()` para o caso inativo.
- Impacto para próximos agentes: Sempre que adicionar um enum de status a uma entidade, ja incluir os metodos de transicao publica necessarios para o dominio (`Suspend`/`Activate`/`Archive` etc.) — sem isso, testes e futuros fluxos administrativos acabam usando reflexao ou alterando colunas via SQL direto. Manter `Status` `private set` para preservar invariantes; expor apenas transicoes explicitas.

### 2026-06-17 - `docs/specs` passa a ser fonte de verdade de contratos

- Contexto: O projeto ja tinha docs explicativas em `docs/architecture`, `docs/api` e `docs/database`,
  mas faltava uma camada formal de specs para guiar implementacoes por contrato.
- Decisão: Criar `docs/specs/` como fonte de verdade para escopo, dominio, autenticacao,
  checkout, webhooks, Inbox/Outbox, adapters, API, banco, seguranca, observabilidade,
  testes e integracao Job Search. Criar `docs/adr/` para decisoes arquiteturais aceitas.
- Evidência: Specs `000` a `014` e ADRs `ADR-0001` a `ADR-0005` foram adicionados;
  `AGENTS.md`, `README.md` e harness passaram a apontar para specs/ADRs.
- Impacto para próximos agentes: Antes de alterar codigo, leia a spec relacionada.
  Se o codigo divergir da spec, registre o gap e corrija em slice pequeno.
  Mudancas de contrato devem atualizar a spec; decisoes arquiteturais novas devem atualizar/criar ADR.

### 2026-06-17 - Gaps documentais para revisao futura

- Contexto: Durante a formalizacao das specs, foi identificado que a semantica `Created`/`Pending`
  do checkout precisava ser consolidada e que `.env.example` pode aparecer compactado em contextos raw.
- Decisão: Documentar em `docs/specs/004-payment-lifecycle.md` que `Created` e o estado interno
  antes do provider e `Pending` e o estado persistido/retornado apos `checkoutUrl`.
  Nao alterar `.env.example` nesta tarefa docs-only.
- Evidência: `Payment` inicia em `Created`; `CreateCheckoutHandler` chama `AttachProviderResult(..., PaymentStatus.Pending)` antes de salvar a resposta de sucesso.
- Impacto para próximos agentes: Auditorias futuras devem comparar codigo e specs antes de mudar comportamento; se reformatarem `.env.example`, manter apenas valores fake e nao commitar `.env` real.

### 2026-06-17 - Specs de pagamento devem ficar explícitas sobre idempotência e webhooks

- Contexto: A auditoria encontrou gaps onde o texto da spec estava correto em alto nivel,
  mas faltavam detalhes operacionais para impedir fallback silencioso de provider,
  replay com payload diferente e webhook orfao marcado como processado.
- Decisão: Checkout compara `request_hash` antes de retornar replay,
  provider explicito nunca cai para outro provider, webhooks sao parseados pelo adapter
  e pagamento inexistente vira `Failed` com `last_error`.
- Evidência: Testes unitarios cobrem conflito de idempotencia, provider explicito invalido/sem conta ativa,
  transicoes perigosas de `PaymentStatus`, parsing por adapter, webhook orfao
  e HMAC `{timestamp}.{rawBody}`.
- Impacto para próximos agentes: Ao alterar checkout ou webhook, confira specs `004`, `005`, `006`, `008`, `011` e `014`;
  preserve `eventId` como id do `OutboxEvent` e nao marque webhook sem pagamento como `Processed`.

### 2026-06-17 - Webhook interno usa eventId, HMAC com timestamp e attempts por resultado financeiro

- Contexto: A auditoria final separou tres riscos: consumidor sem chave estavel de idempotencia,
  HMAC sem contrato anti-replay completo e `PaymentAttemptStatus.Succeeded` usado para status negativos.
- Decisão: Documentar `eventId` como id estavel do `OutboxEvent`,
  assinar `{timestamp}.{rawBody}` com HMAC-SHA256 em hex lowercase
  e mapear webhooks `Rejected`, `Failed`, `Expired` e `Cancelled` para `PaymentAttemptStatus.Failed`.
- Evidência: Specs `003`, `004`, `006`, `007`, `009`, `011` e `014` foram alinhadas;
  testes de webhook cobrem `Approved`, `Rejected` e `Failed`.
- Impacto para próximos agentes: Nao confundir webhook processado com pagamento aprovado.
  Preserve `eventId` nos retries do mesmo outbox e valide HMAC sobre body bruto, sem reserializar JSON.

### 2026-06-16 - OpenCode rejeita chaves top-level `agents` e `notes`

- Contexto: A execução de `opencode` abortava porque `.opencode/opencode.json` continha as chaves top-level `agents` e `notes`.
- Decisão: Manter `opencode.json` apenas com chaves aceitas pelo schema instalado; preservar referências de agentes em `.opencode/agents/*.md` e documentar a convenção em `.opencode/README.md`.
- Evidência: A CLI reportou `Configuration is invalid ... Unrecognized keys: agents, notes`; os tipos locais do SDK indicam `agent` no singular como chave de configuração válida.
- Impacto para próximos agentes: Não adicionar `agents` ou `notes` no topo de `.opencode/opencode.json`; validar mudanças de OpenCode com `opencode debug config` ou comando equivalente.

### 2026-06-18 - Endpoints autenticados devem derivar tenant/application de ITenantContext; nunca aceitar do body

- Contexto: Slice 6-B fechou o gap P1-2. Antes do slice, `RegisterProviderAccountHandler` construia `ProviderAccount` a partir de `request.TenantId`/`request.ApplicationId` vindos do body, ignorando `ITenantContext`. Risco: cross-tenant registration por uma application autenticada. Depois do slice, `tenantId`/`applicationId` sao parametros explicitos do handler, injetados pelo controller via `ITenantContext`, e o DTO do body nao expoe mais esses campos.
- Decisao: Regra preferencial aplicada: remover `tenantId`/`applicationId` do `RegisterProviderAccountRequestDto` em vez de mante-los deprecated. ASP.NET Core ignora silenciosamente chaves extras no JSON, entao o simples fato de o DTO nao declarar a propriedade ja garante que valores divergentes nao chegam ao handler. Controller chama `ITenantContext.TenantId`/`ApplicationId` dentro de `try/catch (InvalidOperationException) => Unauthorized(...)`. Handler rejeita `Guid.Empty` com `InvalidOperationException` e nao persiste quando o caller nao fornece IDs validos. Padrao igual ao `CheckoutsController` (ja usa `ITenantContext`).
- Evidencia: 15 testes em `tests/PaymentHub.UnitTests/Application/RegisterProviderAccountHandlerTests.cs` (10) e `tests/PaymentHub.UnitTests/Api/ProviderAccountsControllerTests.cs` (5). Verificacoes: tipo do DTO nao expoe `TenantId`/`ApplicationId` (compile-time via `Type.GetProperty`); tipo da resposta nao expoe `ApiKey`/`Secret`/`EncryptedCredentials`; handler usa IDs do caller; body com campos extras e divergentes nao afeta operacao; contexto ausente retorna 401 sem chamar handler; validacao falha retorna 400 sem consultar contexto. Build limpo e 85 testes passando. Report em `docs/audits/slice-6b-provider-account-authenticated-context-report-2026-06-18.md`.
- Impacto para próximos agentes: Sempre que criar/alterar um endpoint autenticado server-to-server, seguir o padrao: (1) DTO de request nao deve conter `tenantId`/`applicationId`; (2) controller injeta `ITenantContext`, le os valores via propriedade (com `try/catch` em `InvalidOperationException` retornando 401), passa-os explicitamente ao handler; (3) handler confia nos parametros explicitos e rejeita `Guid.Empty`. Isso evita o vetor de cross-tenant/cross-application registration e impede que o body seja usado como vetor de bypass. `ITenantContext` e a fonte unica de tenant/application em fluxos autenticados; nao criar mecanismo paralelo. Ao auditar novos endpoints, conferir `009-api-contracts.md` e `002-multitenancy-and-authentication.md` para garantir que a regra esteja documentada.

### 2026-06-16 - Scaffold do Payment Gateway MVP criado com .NET 10 + Clean Architecture

- Contexto: Tarefa inicial para criar a base multitenant do gateway, cobrindo Domain/Application/Infrastructure/Api/Worker, Inbox/Outbox, idempotência e adapter `FakePaymentProvider`.
- Decisão: Adotar `net10.0` em todos os projetos, EF Core 10 + Npgsql 10,
  Swashbuckle 6.8.1 (compatível com `Microsoft.OpenApi 1.6.22`),
  `FluentValidation 11.11.0`, Serilog, override explícito de `System.Security.Cryptography.Xml 10.0.9`
  para silenciar vulnerabilidade transitiva e migrations geradas via `dotnet-ef 9.0.2`
  (rodar com `DOTNET_ROOT=/usr/lib/dotnet`).
- Evidência: `dotnet build PaymentHub.slnx` resulta em 0 erros / 0 warnings em 9 projetos; `dotnet test` executa 49 testes unitários; `docker compose config` valida o compose.
- Impacto para próximos agentes: A migration inicial
  (`src/PaymentHub.Infrastructure.Postgres/Migrations/*_InitialSchema.cs`)
  deve ser aplicada com `dotnet ef database update` antes de subir a API.
  Não usar `Microsoft.OpenApi 2.0.0` até que Swashbuckle atualize para acomodar o novo namespace;
  até lá mantenha Swashbuckle 6.8.x.
  Ao rodar `dotnet ef` em ambientes com runtimes múltiplos, defina `DOTNET_ROOT` antes do comando.

### 2026-06-16 - `Money` exige ValueConverter para Postgres

- Contexto: Ao rodar `dotnet ef migrations add InitialSchema` o tooling reportou que `Payment.Amount` (tipo `Money`) não pode ser mapeado.
- Decisão: Criar `MoneyToLongConverter` (em `PaymentHub.Infrastructure.Postgres.Configurations`)
  para mapear `Money` para `long` (valor em centavos)
  e configurar a coluna `amount_in_cents` no `PaymentConfiguration`.
- Evidência: Migration gerada com sucesso; testes unitários de `Payment` continuam passando usando `Money.Of(...)`.
- Impacto para próximos agentes: Sempre que criar um value object novo, adicionar o conversor em `Configurations/`
  para que o EF Core consiga mapear.
  Manter `Money.Of` como porta de entrada para evitar criar instâncias inválidas.

### 2026-06-16 - `dotnet new sln` gera `slnx` no SDK 10

- Contexto: O comando `dotnet new sln` no SDK 10.0.109 cria `PaymentHub.slnx` (XML)
  em vez de `.sln` legado, e a maioria das referências esperava `.sln`.
- Decisão: Manter `PaymentHub.slnx` na raiz e usar `dotnet build PaymentHub.slnx`
  / `dotnet test` (sem args) para descobrir a solução.
  Em pipelines legadas, executar `dotnet sln migrate` para gerar `.sln` se necessário.
- Evidência: `dotnet build PaymentHub.slnx` resolve 9 projetos; `dotnet test` descobre automaticamente o projeto de teste.
- Impacto para próximos agentes: Documentar nos runbooks a referência `slnx` e atualizar scripts CI para usar `slnx` (ou rodar `dotnet sln migrate` antes).

### 2026-06-16 - Workers e API compartilham DbContext, mas usam serviços diferentes

- Contexto: API e Worker precisam consultar/alterar o mesmo banco, mas cada um deve expor apenas os serviços necessários.
- Decisão: API registra `HttpApplicationWebhookDispatcher` (HttpClient real com HMAC);
  Worker registra `NoopApplicationWebhookDispatcher` que apenas loga o evento.
  A separação por processo impede vazamento de dispatcher HTTP em ambiente que não deve entregar webhooks
  (ex.: worker de leitura).
- Evidência: `Program.cs` do Worker injeta `NoopApplicationWebhookDispatcher` e usa `services.AddHttpClient` apenas para o cliente nomeado; `Program.cs` da API injeta `HttpApplicationWebhookDispatcher`.
- Impacto para próximos agentes: Para evoluir para um serviço dedicado de dispatch,
  criar novo projeto `PaymentHub.Dispatcher` reaproveitando `IApplicationWebhookDispatcher`.
  Não tentar acoplar Worker e API no mesmo processo.

### 2026-06-27 - Slice 2-A: AbacatePay transparente PIX sandbox usa resolver-as-value-object, IOptionsMonitor + named HttpClient + Singleton client, e safe-envelope pattern

- Contexto: Ate o Slice 2-A (2026-06-27), `AbacatePayProviderAdapter` ainda era skeleton (`Success=false` hard-coded). `IPaymentProviderAdapter` era Singleton e nao podia receber repository scoped; `CreateCheckoutHandler.ResolveProviderAsync` resolvia `ProviderAccount` para validar existencia e descartava a entidade, impossibilitando o adapter de receber credenciais. `CreateCheckoutProviderRequest` nao carregava dados de credencial. `PaymentStatusMapper.MapAbacatePay` existia mas sem testes explicitos para os status da doc oficial (`PENDING`, `PAID`, `EXPIRED`, `CANCELLED`, `FAILED`, `REFUNDED`, `REDEEMED`, `UNDER_DISPUTE`). Fase 2 tinha apenas o Fake funcional e nenhum provider real.
- Decisão: (1) **Resolver-as-value-object**: `ResolveProviderAsync` passou a retornar um record `internal ResolvedProvider` com `ProviderCode`, `ProviderAccountId`, `Environment`, `EncryptedCredentials`. Factories estaticas `FromAccount(ProviderAccount)` e `DevFallback(ProviderCode)` cobrem o caminho feliz e o fallback de Development. **Apos a resolucao, a credencial e propagada via request DTO, nao via injecao no adapter.** (2) **CreateCheckoutProviderRequest estendido com 3 init-only opcionais** (`ProviderAccountId`, `ProviderEnvironment`, `ProtectedCredentials`) **sem alterar o construtor posicional** — adapters Fake/Stripe/MercadoPago continuam compilando porque so leem os primeiros 12 argumentos; consumers novos (AbacatePay) leem as init-only via `init`. (3) **Pattern outbound typed client + IOptionsMonitor + IHttpClientFactory nomeado**: `IAbacatePayClient` depende apenas de `IHttpClientFactory` (Singleton-safe), `IOptionsMonitor<AbacatePayOptions>` (Singleton-safe) e `ILogger<>` (Singleton-safe). O adapter continua Singleton. `HttpClient` e resolvido por nome (`"abacatepay"`) com `BaseAddress` derivado de `AbacatePayOptions.BaseUrl` e `Timeout` derivado de `AbacatePayOptions.TimeoutSeconds`. **`HttpClient` direto causa socket exhaustion** (ja documentado em learnings do Slice 7-A.3); o factory gerencia o pool corretamente. (4) **Bearer Token via `IHttpClientFactory`**: `request.Headers.Authorization = new AuthenticationHeaderValue("Bearer", apiKey)`. A chave nunca e persistida nem logada; o adapter chama `ICredentialProtector.Unprotect` uma unica vez e o JSON desprotegido (`{apiKey, secret}`) e descartado apos extrair o `apiKey`. (5) **Safe-envelope + categorized-error pattern**: `AbacatePayEnvelope<T>` modela `{ data, success, error? }`. `AbacatePayClient` valida `success=true` antes de retornar e lanca `AbacatePayClientException(EnvelopeFailure)` caso contrario. Categorias: `BadRequest`, `Unauthorized`, `NotFound`, `RateLimited`, `ServerError`, `Network`, `Timeout`, `EnvelopeFailure`, `Unexpected`, `SimulationDisabled`. `IsTransient` e derivado da categoria (true para `RateLimited`/`ServerError`/`Network`/`Timeout`). **Mensagem da exception NAO inclui API key, body, brCodeBase64, response body** — so categoria + statusCode opcional. Caller `CancellationToken` cancelado propaga como `OperationCanceledException`, distinguindo cancelamento do operador de timeout do `HttpClient`. (6) **Simulate gated by config**: `AllowDevModeSimulation` (default `false` em `appsettings.json`, `true` em `appsettings.Development.json`); o client lanca `SimulationDisabled` **antes** de montar request quando flag false — preferencia explicita por "falhar cedo". (7) **`BaseAddress` path gotcha**: `BaseAddress = new Uri(baseUrl.TrimEnd('/') + "/")` com path `/transparents/create` (leading slash) faz `HttpClient` resolver do host root, **ignorando** `/v2/`. Correcao: caminhos **sem** leading `/` (ex.: `transparents/create`), preservando o segmento `/v2/` do `BaseAddress`. (8) **Status mapping estendido com decisao documentada**: `redeemed → Approved` (PIX ja resgatado conta como pago final), `under_dispute → Pending` (decisao explicita: ate existir anti-fraude chargeback, mantemos intermedio). Testes cobrem todos os 11 status do mapping mais case-insensitive do provider code. (9) **`AesCredentialProtector` constructor exige configuration**: testes nao podem instanciar diretamente sem `PaymentHub:CredentialEncryptionKey`. Solucao: `FakeCredentialProtector` em `tests/Support/` (mesmo pattern de `FakeWebhookSecretProtector`) com marker prefix reversivel, **nao criptografico**, usado apenas em testes de adapter.
- Evidência: 9 arquivos novos em `src/PaymentHub.Infrastructure.Providers/AbacatePay/` (`AbacatePayOptions.cs`, `AbacatePayErrorCategory.cs`, `AbacatePayClientException.cs`, `IAbacatePayClient.cs`, `AbacatePayClient.cs`, `Models/AbacatePayEnvelope.cs`, `Models/AbacatePayCustomerRequest.cs`, `Models/AbacatePayCreateTransparentPixRequest.cs`, `Models/AbacatePayCreateTransparentPixResponse.cs`, `Models/AbacatePayCheckTransparentPixResponse.cs`, `Models/AbacatePaySimulatePaymentResponse.cs`). 5 arquivos produtivos alterados (`AbacatePayProviderAdapter.cs` reescrito, `ProvidersServiceCollectionExtensions.cs` com options + named HttpClient + Singleton client, `ProviderModels.cs` com 3 init-only, `CreateCheckoutHandler.cs` com `ResolvedProvider` record, `PaymentStatusMapper.cs` com 2 status adicionados). 2 arquivos `appsettings*.json` com secao `Providers:AbacatePay`. 1 test support novo (`FakeCredentialProtector.cs`). 2 arquivos de teste novos (`AbacatePayClientTests.cs` 40 testes, `AbacatePayProviderAdapterTests.cs` 17 testes). Suite: 348 testes (baseline 291 + 57 novos), 0 warnings, 10 integracao Postgres preservados. `scripts/agent-architecture-check.sh` + `scripts/agent-docs-check.sh` + `git diff --check` verdes. Relatorio em `docs/audits/slice-2a-abacatepay-sandbox-report-2026-06-26.md`.
- Impacto para proximos agentes: (a) Ao evoluir adapters para provedores com credenciais (Stripe, MercadoPago futuros), copiar o pattern: **credentials via request DTO init-only** + **`ICredentialProtector.Unprotect` no adapter** + **`Bearer Token` via `IHttpClientFactory` named client** + **envelope + categorized exception** + **mensagem segura**. NUNCA injetar `IProviderAccountRepository` em adapter Singleton — passe a credencial ja resolvida. (b) Ao adicionar novo provider outbound, criar `IProviderClient` em `Infrastructure.Providers/<Provider>/` com metodos ReceiveProviderAccountProtectedCredentials como parametro explicito; **NAO armazenar credencial em campo da classe**. (c) Ao adicionar opcoes de outbound, seguir pattern `ProviderOptions.cs` em `Infrastructure.Providers/<Provider>/` com `SectionName` + `BaseUrl` + `TimeoutSeconds` + flags opt-in booleanos. Binging via `services.Configure<TOptions>(...)` em `AddPaymentHubProviders`; nunca via `IConfiguration` direto no construtor. (d) **NUNCA usar path starting com `/` quando `BaseAddress` ja tem path** (`/v2/`) — usar path relativo preserva o segmento do base. Gotcha foi causa de 2 testes falhando antes da correcao. (e) **Caller `CancellationToken` cancelado != timeout do `HttpClient`**: o primeiro propaga `OperationCanceledException`, o segundo e envelopado em `AbacatePayClientException(Timeout, isTransient=true)`. Distinguir explicitamente no `catch`. (f) Ao adicionar novo status bruto no `PaymentStatusMapper`, **documentar a decisao de mapeamento em teste** (ex.: `under_dispute -> Pending` foi decidido por risco operacional, nao por equivalencia exata). (g) Ao cobrir caminho de "no credentials", garantir que o adapter retorna `Success=false` com mensagem segura ("AbacatePay requires ProviderAccount with encrypted credentials.") **antes** de chamar o client — evita stack trace vazando a regra de credencial. (h) Webhooks externos completos (HMAC + event normalization) seguem em **Slice 2-B** (a abrir); o `ParseWebhookAsync` atual no adapter faz parsing JSON naive e NAO verifica assinatura. (i) **`AesCredentialProtector` exige config**; em testes, usar `FakeCredentialProtector` (mesmo padrao de `FakeWebhookSecretProtector`). Marker `fake-cred|` distingue blob fake de blob real em `persisted.X.StartsWith("fake-cred|")` assertions.

# Feature List

Backlog leve para agentes. Use para registrar itens que nao serao implementados no slice atual.

| ID | Tipo | Titulo | Status | Spec/ADR | Observacoes |
|----|------|--------|--------|----------|-------------|
| AI-001 | Harness | Criar CI com restore/build/test | Concluido | `docs/ai/validation-checklist.md` | Workflow `.github/workflows/ci.yml` valida harness, restore, build e test. |
| AI-002 | Testes | Evoluir testes de integracao | Aberto | `docs/specs/013-testing-strategy.md` | Projeto existe, mas precisa cenarios reais. |
| AI-003 | Testes | Avaliar smoke/E2E local | Concluido | `docs/specs/009-api-contracts.md` | Slice 3-IT (2026-06-29): 4 testes E2E cobrindo checkout + webhook valido + idempotencia + fail-fast 401 com API real + Postgres (Testcontainers) + adapter AbacatePay + fakes de transporte outbound. |
| AI-004 | Harness | Aprimorar CI com cache, resultados e gate de secrets | Concluido | `docs/ai/validation-checklist.md` | CI usa cache NuGet, publica `.trx` e roda `scripts/agent-verify.sh` com scan simples de secrets. |
| AI-005 | Harness | Alinhar agentes OpenCode ao harness Copilot/Codex | Concluido | `AGENTS.md` | `.opencode/README.md` e agentes apontam para Copilot instructions, specs, agents equivalentes e validações. |
| AI-006 | Docs | Documentar uso diario do harness | Concluido | `docs/ai/harness-engineering.md` | README e docs IA indicam `agent-init`, prompts, agents, skills, progresso e validacao. |
| AI-007 | Auditoria | Rodar proxima auditoria specs versus codigo | Concluido | `docs/audits/spec-adherence-refresh-2026-06-24.md` | Auditoria curta executada sem corrigir produto; gaps viraram backlog. |
| AI-008 | Segurança | Avaliar secret scanning dedicado no CI | Aberto | `docs/specs/011-security-and-compliance.md` | Considerar Gitleaks ou ferramenta equivalente alem do scan simples atual. |
| AI-009 | Harness | Evoluir OpenCode com agentes, skills e checks locais | Concluido | `docs/harness/opencode.md` | `opencode.json` usa `agent`, skills locais, permissao segura e scripts docs/architecture/smoke. |
| PH-SEC-001 | Segurança | Proteger `ApplicationClient.WebhookSecret` em repouso | Concluido | `docs/specs/011-security-and-compliance.md` / `docs/adr/ADR-0007-webhook-secret-protection.md` | Slices 6-C (2026-06-25) + 7-A.6 (2026-06-26) + 7-A.9 (2026-06-26). |
| PH-WORKER-001 | Worker | Substituir dispatcher no-op do Outbox worker | Concluido | `docs/specs/007-inbox-outbox-workers.md` / `docs/adr/ADR-0010-real-outbox-dispatcher-location.md` | Slice 7-A (2026-06-26, sub-slices 7-A.1 a 7-A.9). |
| PH-OUTBOX-E2E | Testes | Suite E2E do ciclo Outbox → ApplicationClient webhook (dispatcher real + Postgres real + API real) | Concluido | `docs/specs/007-inbox-outbox-workers.md`, `docs/specs/013-testing-strategy.md`, `docs/specs/011-security-and-compliance.md` | Slice 7-IT CONCLUIDO 2026-06-30 — 7 testes em `tests/PaymentHub.IntegrationTests/EndToEnd/OutboxDispatcherE2ETests.cs` (P1: Sent, HMAC, 500 retry, 429 retry, UnprotectFailure; P2: AbacatePay full flow, no-redispatch de Sent). Adicionado `InternalsVisibleTo("PaymentHub.IntegrationTests")` em `PaymentHub.Worker.csproj` para invocar `DispatchOnceAsync` manualmente. `ApplicationWebhookCaptureHandler` evoluido com `EnqueueResponse` (fila FIFO de respostas programaveis, default 204) + helper puro `InternalWebhookHmac.Compute/Matches`. Detalhes em `docs/audits/slice-7-it-outbox-dispatcher-e2e-report-2026-06-30.md`. |
| PH-OUTBOX-MULTI-INSTANCE | Worker | Claim transacional multi-instancia com `FOR UPDATE SKIP LOCKED` + sweep automatico de `Processing` orfao | Concluido | `docs/specs/007-inbox-outbox-workers.md`, `docs/specs/011-security-and-compliance.md`, `docs/specs/010-database-contract.md` | Slice 7-M1 CONCLUIDO 2026-06-30. (1) `OutboxRepository.ClaimPendingForDispatchAsync(int batchSize, DateTime now, ct)` raw `NpgsqlConnection` + `BeginTransactionAsync(ReadCommitted)` + `SELECT ... FOR UPDATE SKIP LOCKED LIMIT @batchSize` + atomic `UPDATE status='Processing', processing_started_at=@now` em uma unica transacao (EF Core 10 nao traduz `SKIP LOCKED` em LINQ). (2) `OutboxRepository.SweepOrphanedProcessingAsync(DateTime cutoff, ct)` single `ExecuteSqlRawAsync` UPDATE persistindo apenas o literal `'ProcessingOrphaned'` em `last_error`. (3) `OutboxEvent.ProcessingStartedAt` (`timestamptz NULL`) e nova categoria enum `WebhookDispatcherCategory.ProcessingOrphaned (8)`. (4) Migration `20260630184619_AddOutboxProcessingStartedAtAndIndexes`. (5) `OutboxDispatcherWorker.DispatchOnceAsync` roda sweep antes do claim; sanity-check anti-regressao contra remocao do UPDATE do claim path. (6) `PaymentHubOptions.OutboxProcessingTimeoutSeconds = 900` (default, configuravel). (7) Suite E2E cresceu para 498 testes (467 unit + 31 integration): +2 em `OutboxDispatcherConcurrencyTests` (P1.1 SKIP LOCKED, P1.2 distribuicao entre 3 workers) e +5 em `OutboxProcessingSweepTests` (P1.3-P1.5 sweep, P2.1-P2.2 batch/next_retry_at). `outbox_events.payload` permanece `jsonb` (NAO regressao). Detalhes em `docs/audits/slice-7-m1-outbox-multi-instance-report-2026-06-30.md`. |
| PH-SEC-002 | Segurança | Validar assinatura de webhooks externos reais | Concluido | `docs/specs/006-provider-webhooks.md` | Slice 2-B CONCLUIDO 2026-06-29 — HMAC-SHA256 Base64 + fail-fast 401 + metadata routing para AbacatePay. Stripe/MercadoPago dependem de Phase 4. |
| PH-AUD-001 | Auditoria | Gravar `AuditLog` em acoes administrativas | Aberto | `docs/specs/012-observability-and-audit.md` | Criacao/alteracao de tenants, applications e provider accounts. |
| PH-OBS-001 | Observabilidade | Catalogo `PaymentHubLogEvents` + helpers `SafeLog` + anti-leak regex gate | Concluido | `docs/specs/012-observability-and-audit.md`, `docs/specs/011-security-and-compliance.md` | Slice 9-O1 (2026-07-01). 31 constantes canonicas + 4 helpers (`Id`, `Length`, `Flag`, `Category`) + gate regex em `scripts/agent-docs-check.sh` cobrindo 6 tokens sensitive (`apiKey`, `webhookSecret`, `rawPayload`, `signature`, `Authorization`, `body`). |
| PH-OBS-002 | Observabilidade | `CorrelationIdMiddleware` + accessor + coluna `correlation_id` em `webhook_events` e `outbox_events` | Concluido | `docs/specs/012-observability-and-audit.md`, `docs/specs/011-security-and-compliance.md` | Slice 9-O1 (2026-07-01). Header HTTP `X-Correlation-Id` propagado end-to-end (checkout -> outbox -> webhook outbound). `CorrelationIdMiddleware` registrado ANTES de `ApiKeyAuthenticationMiddleware` em `Program.cs`. Migration `20260701000001_AddObservabilityColumns`. Worker usa `NullCorrelationIdAccessor`. **E2E coberto por Slice 9-O1.IT (2026-07-01)** com 4 testes via Testcontainers + WebApplicationFactory. |
| PH-OBS-006 | Observabilidade | CorrelationId E2E via Testcontainers + WebApplicationFactory | Concluido | `docs/specs/012-observability-and-audit.md` | Slice 9-O1.IT (2026-07-01). 4 testes E2E em `tests/PaymentHub.IntegrationTests/EndToEnd/CorrelationIdE2ETests.cs`: generate-when-absent (middleware gera GUID-N), preserve-when-valid (echo verbatim), replace-when-invalid (substituicao silenciosa), dedup-duplicate-headers (colapsa para um unico). Endpoint `GET /health` (anonymous, sem controller, retorna 404) — middleware corre antes do routing. Per-test fresh `PaymentHubApiFactory` (nao compartilhada). **Bug fix coberto:** migration `20260701000001_AddObservabilityColumns` shipped em `aaa9ea5` SEM `.Designer.cs` — hand-author criado em 9-O1.IT copiando `20260630184619` template + 2 property mappings. Suite 547 unit + 37 integration = **584 testes passing**. Detalhes em `docs/audits/slice-9-o1-it-correlationid-e2e-report-2026-07-01.md`. |
| PH-OBS-003 | Observabilidade | `PaymentHubMetrics` com 13 counters + 3 histograms + tag whitelist | Concluido | `docs/specs/012-observability-and-audit.md` | Slice 9-O1 (2026-07-01). Meter `PaymentHub 1.0.0`. Whitelist runtime de 7 chaves (`provider`, `operation`, `status`, `error_category`, `event_type`, `environment`, `worker`). Instrumentos cobrem checkout, provider webhooks, inbox, outbox, auth. Suite 547 testes (522 baseline + 25 novos). |
| PH-OBS-004 | Observabilidade | Wire `PaymentHubMetrics` em handlers/workers (instrumentacao ativa) | Aberto | `docs/specs/012-observability-and-audit.md` | Slice 9-O2+. Slice 9-O1 introduziu o catalogo e testes; integracao com `CreateCheckoutHandler`, `ProviderWebhooksController`, `ProcessWebhookEventHandler`, `OutboxDispatcherWorker`, `ApiKeyAuthenticationMiddleware` sera feita em slice proprio para preservar tamanho da slice. |
| PH-OBS-005 | Observabilidade | Distributed tracing end-to-end via `Activity`/OpenTelemetry | Aberto | `docs/specs/012-observability-and-audit.md` | Phase 9 / 9-O2+. Nao cobre MVP; CorrelationId + Logs estruturados cobrem incident triage. |
| PH-PROVIDER-WEBHOOK-ABACATEPAY | Provider | Webhook externo AbacatePay com HMAC + normalizacao | Concluido | `docs/specs/006-provider-webhooks.md`, `docs/specs/008-provider-adapters.md`, `docs/specs/011-security-and-compliance.md` | Slice 2-B CONCLUIDO 2026-06-29. 78 testes novos distribuidos entre verifier, normalizer, adapter, handler e controller. Phase 2 passa a `IMPLEMENTED`. |
| PH-PROVIDER-WEBHOOK-MGT-2C | Provider | Endpoints PUT/GET para gerenciar inscricao de webhook AbacatePay via API | Concluido | `docs/specs/008-provider-adapters.md`, `docs/specs/009-api-contracts.md`, `docs/specs/011-security-and-compliance.md` | Slice 2-C CONCLUIDO 2026-06-30. 4 colunas non-sensitive em `provider_accounts` + `IProviderWebhookManagementClient` abstraction + feature flag `Providers:AbacatePay:AllowWebhookRegistration` (default `false`, opt-in). 59 unit tests + 3 integration tests = +62 net. Cliente HTTP real entregue pela Slice 2-C.1 (substitui o `NoOpProviderWebhookManagementClient`). |
| PH-PROVIDER-WEBHOOK-MGT-CLIENT | Provider | Client HTTP real para `POST /webhooks/create` na AbacatePay (substitui `NoOpProviderWebhookManagementClient`) | Concluido | `docs/specs/008-provider-adapters.md`, `docs/specs/011-security-and-compliance.md` | Slice 2-C.1 CONCLUIDO 2026-06-30. (1) `AbacatePayWebhookManagementClient` real com 4-gate pipeline (provider check + feature flag + pre-flight validation + apiKey extraction via `IProviderAccountCredentialsReader`). (2) Named HttpClient dedicado `abacatepay-webhooks` (distinto de `abacatepay` que serve transparent-PIX). (3) Categorizacao de erros reusa `AbacatePayClientException` / `AbacatePayErrorCategory` (400→BadRequest, 401/403→Unauthorized, 404→NotFound, 429→RateLimited, 5xx→ServerError, `HttpRequestException`→Network, `TaskCanceledException` sem caller cancel→Timeout; nova categoria `RegistrationDisabled = 11` adicionada mas nao usada pelo client real). (4) `IProviderAccountCredentialsReader` (public interface em `Application/Abstractions/Security/`) + adapter em `Infrastructure.Postgres/Security/` + `<InternalsVisibleTo Include="PaymentHub.Infrastructure.Postgres" />` no `PaymentHub.Application.csproj`. (5) `NoOpProviderWebhookManagementClient` removido (registro unico substitui). (6) No-leak guarantees: apiKey extraido em memoria apenas, webhookSecret enviado ao upstream mas nunca logado/retornado, logs so' carregam `providerCode` + `endpoint.Length` + `eventCount` + `category` + `statusCode`. (7) Suite cresceu para 522 testes (489 unit + 33 integration): +20 em `AbacatePayWebhookManagementClientTests` (17 scenarios + 3 theory cases) + 2 em `ProviderAccountsWebhookControllerTests` (Deferred / Registered) + 1 em `AbacatePayWebhookManagementE2ETests` (full E2E via `PaymentHubApiFactory` + Postgres Testcontainer + `AbacatePayFakeHttpHandler` estendido). Detalhes em `docs/audits/slice-2c1-abacatepay-webhook-management-client-report-2026-06-30.md`. |
| PH-PROVIDER-WEBHOOK-EVENTS-TEXT | Banco | Manter `provider_accounts.webhook_events` como `text` (NAO `jsonb`) | Concluido | `docs/specs/010-database-contract.md` | **⚠️ ANTI-REGRESSION (mirror de PH-PROVIDER-WEBHOOK-RAWPAYLOAD-TEXT).** Slice 2-C (2026-06-30) reproduziu o mesmo bug do Slice 3-IT: `jsonb` reformata JSON no insert (espaco apos `:` e `,`), quebrando round-trip byte-exact. Migration `20260630001726_AddProviderAccountWebhookColumns` regenerada para usar `text`. **NUNCA** reverter para `jsonb`. Cobertura: `ProviderAccountWebhookPersistenceTests.ProviderAccount_ShouldPersistAllWebhookConfigurationColumns`. Detalhes em `docs/audits/slice-2c-abacatepay-webhook-management-report-2026-06-30.md` (Anti-Regression Rule 1). |
| PH-PROVIDER-WEBHOOK-E2E | Testes | Testes E2E do fluxo AbacatePay (API + Postgres + adapter + webhook) | Concluido | `docs/specs/013-testing-strategy.md`, `docs/specs/006-provider-webhooks.md` | Slice 3-IT CONCLUIDO 2026-06-29 — 4 testes P1 em `tests/PaymentHub.IntegrationTests/EndToEnd/AbacatePayCheckoutE2ETests.cs`. |
| PH-PROVIDER-WEBHOOK-RAWPAYLOAD-TEXT | Banco | Migrar `webhook_events.raw_payload` de `jsonb` para `text` (HMAC byte preservation) | Concluido | `docs/specs/010-database-contract.md` | **⚠️ ANTI-REGRESSION (BLOCKER for Phase 7-IT).** Slice 3-IT (2026-06-29) — bug encontrado por E2E testing. Postgres `jsonb` reformata JSON no insert (espacos, normaliza chaves), o que quebra HMAC sobre o body bruto. Migracao `20260629205545_ChangeRawPayloadToText` aplicada. **NUNCA** voltar a coluna para `jsonb`. Cobertura: `ProviderWebhook_ValidSignature_UpdatesPaymentAndEnqueuesOutbox`. Detalhes completos em `docs/audits/slice-3-it-e2e-api-postgres-outbox-provider-report-2026-06-29.md` (seção "Anti-Regression Notes" Rule 1). |
| PH-PROVIDER-WEBHOOK-ATTEMPT-TRACKING | Provider | Forcar `Add` explicito do novo `PaymentAttempt` no `ProcessWebhookEventHandler` | Concluido | `docs/specs/006-provider-webhooks.md` | **⚠️ ANTI-REGRESSION (BLOCKER for Phase 7-IT).** Slice 3-IT (2026-06-29) — bug encontrado por E2E testing. Collection navigation tracking do EF Core 10 nao detecta confiavelmente itens adicionados via `entity.Navigation.Add(item)`; o item aparecia como `Modified` em vez de `Added`, levantando `DbUpdateConcurrencyException`. Solucao: chamar `_payments.AddAttemptAsync(attempt, ct)` apos `payment.RegisterAttempt(...)`. **NUNCA** remover a chamada explicita. Cobertura: `ProviderWebhook_ValidSignature_UpdatesPaymentAndEnqueuesOutbox`. Detalhes completos em `docs/audits/slice-3-it-e2e-api-postgres-outbox-provider-report-2026-06-29.md` (seção "Anti-Regression Notes" Rule 2). |
| PH-DB-001 | Banco | Decidir FKs obrigatorias versus referencias logicas | Aberto | `docs/specs/010-database-contract.md` | Atualizar spec/ADR antes de migrations. |
| DOC-001 | Docs | Alinhar overview ao HMAC interno atual | Aberto | `docs/specs/011-security-and-compliance.md` | Usar contrato `{timestamp}.{rawBody}` com headers atuais. |

## Convenções

- Status sugeridos: `Aberto`, `Planejado`, `Em progresso`, `Bloqueado`, `Concluido`.
- Cada item deve apontar spec, ADR ou doc de harness quando existir.
- Nao use este arquivo como substituto de issue tracker quando houver GitHub Issues.
